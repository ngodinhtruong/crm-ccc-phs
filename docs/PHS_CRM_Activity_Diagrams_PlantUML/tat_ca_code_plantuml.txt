===== 00_danh_muc_so_do.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Danh mục Activity Diagram của hệ thống PHS CRM

start
:01. Đăng nhập và điều hướng theo quyền;
:02. Tạo người dùng và cấp quyền;
:03. Tạo khách hàng / doanh nghiệp và tài khoản;
:04. Tạo Ticket thủ công;
:05. Tiếp nhận, phân công và xử lý Ticket;
:06. Quét SLA, cảnh báo và tự đóng Ticket;
:07. Đồng bộ Chatbot và tự động tạo Ticket;
:08. Import và phân loại lỗi bên ngoài;
:09. Ghi nhận cuộc gọi Sale Admin;
:10. Khởi tạo và kích hoạt kỳ KPI;
:11. Thiết lập chỉ tiêu KPI;
:12. Tính điểm và xếp hạng KPI;
stop
@enduml


===== 01_dang_nhap_phan_quyen.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Activity 01 - Đăng nhập và điều hướng theo quyền

|Người dùng|
start
:Nhập tên đăng nhập và mật khẩu;
:Bấm **Đăng nhập**;

|Frontend Next.js|
:Kiểm tra dữ liệu bắt buộc;
if (Dữ liệu hợp lệ?) then (Có)
  :Gửi yêu cầu đăng nhập;
else (Không)
  :Hiển thị lỗi nhập liệu;
  stop
endif

|Backend Django|
:Xác thực tài khoản;
if (Thông tin đăng nhập đúng?) then (Có)
  :Tạo access token và refresh token;
  :Trả token cho frontend;
else (Không)
  :Trả lỗi xác thực;
  |Frontend Next.js|
  :Hiển thị thông báo đăng nhập thất bại;
  stop
endif

|Frontend Next.js|
:Lưu token;
:Gọi API lấy thông tin người dùng hiện tại;

|Backend Django|
:Lấy role, permission, scope và nhóm nghiệp vụ;
:Trả hồ sơ người dùng;

|Frontend Next.js|
if (Có quyền truy cập hệ thống?) then (Có)
  if (Nhóm nghiệp vụ chính?) then (CCC / System)
    :Điều hướng Ticket Dashboard / Workspace;
  elseif (Sale Admin) then (SA)
    :Điều hướng Sale Admin Dashboard;
  else (KPI)
    :Điều hướng KPI cá nhân / KPI Admin;
  endif
else (Không)
  :Xóa token;
  :Hiển thị lỗi không có quyền truy cập;
endif
stop
@enduml


===== 02_tao_nguoi_dung_cap_quyen.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Activity 02 - Tạo người dùng và cấp quyền truy cập

|Quản trị viên|
start
:Mở màn hình tạo người dùng;
:Chọn nhân viên có sẵn hoặc nhập nhân viên mới;
:Nhập username, email và mật khẩu;
:Chọn nhóm nghiệp vụ, role và phạm vi truy cập;

|Frontend Next.js|
:Kiểm tra trường bắt buộc;
if (Dữ liệu hợp lệ?) then (Có)
  :Gửi yêu cầu tạo người dùng;
else (Không)
  :Hiển thị lỗi nhập liệu;
  stop
endif

|Backend Django|
:Kiểm tra quyền quản trị tài khoản;
if (Có quyền?) then (Có)
else (Không)
  :Trả lỗi 403;
  |Frontend Next.js|
  :Hiển thị không có quyền;
  stop
endif

|Backend Django|
:Kiểm tra username và email trùng;
if (Đã tồn tại?) then (Có)
  :Trả lỗi dữ liệu trùng;
  |Frontend Next.js|
  :Hiển thị lỗi;
  stop
else (Không)
endif

|Backend Django|
if (Chọn nhân viên có sẵn?) then (Có)
  :Liên kết User với Employee;
else (Không)
  :Tạo Employee mới;
  :Liên kết User với Employee;
endif
:Tạo tài khoản User;
:Tạo UserRole;
if (Scope = BRANCH / MULTI_BRANCH?) then (Có)
  :Tạo các bản ghi BranchAccess;
else (Không)
endif
if (Có permission riêng?) then (Có)
  :Gán permission bổ sung;
else (Không)
endif
:Ghi nhận thông tin tạo tài khoản;
:Trả kết quả thành công;

|Frontend Next.js|
:Hiển thị tài khoản vừa tạo;
stop
@enduml


===== 03_tao_khach_hang_tai_khoan.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Activity 03 - Tạo khách hàng / doanh nghiệp và tài khoản lưu ký

|Nhân viên|
start
:Mở màn hình tạo mới;
if (Loại đối tượng?) then (Khách hàng cá nhân)
  :Nhập mã KH, họ tên, liên hệ, chi nhánh và PIC;
else (Doanh nghiệp)
  :Nhập mã công ty, tên, liên hệ, chi nhánh và PIC;
endif
:Nhập số tài khoản lưu ký nếu có;

|Frontend Next.js|
:Kiểm tra dữ liệu bắt buộc và định dạng;
if (Dữ liệu hợp lệ?) then (Có)
  :Gửi yêu cầu tạo mới;
else (Không)
  :Hiển thị lỗi nhập liệu;
  stop
endif

|Backend Django|
:Kiểm tra quyền tạo khách hàng;
:Kiểm tra mã khách hàng / mã công ty trùng;
if (Mã đã tồn tại?) then (Có)
  :Trả lỗi trùng dữ liệu;
  |Frontend Next.js|
  :Hiển thị lỗi;
  stop
else (Không)
endif

|Backend Django|
if (Có số tài khoản?) then (Có)
  :Chuẩn hóa và kiểm tra số tài khoản;
  if (Số tài khoản đã tồn tại?) then (Có)
    :Trả lỗi trùng số tài khoản;
    |Frontend Next.js|
    :Hiển thị lỗi;
    stop
  else (Không)
  endif
else (Không)
endif

|Cơ sở dữ liệu|
:Tạo Customer hoặc Company;
if (Có số tài khoản?) then (Có)
  :Tạo CustomerAccount;
else (Không)
endif
:Lưu quan hệ chi nhánh và nhân viên phụ trách;

|Backend Django|
:Trả dữ liệu đối tượng vừa tạo;
|Frontend Next.js|
:Hiển thị thông báo thành công;
:Chuyển đến danh sách / trang chi tiết;
stop
@enduml


===== 04_tao_ticket_thu_cong.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Activity 04 - Tạo Ticket thủ công

|Nhân viên CCC|
start
:Mở màn hình tạo Ticket;
:Chọn khách hàng / doanh nghiệp;
:Chọn tài khoản liên quan;
:Nhập tiêu đề, nội dung và nguồn tiếp nhận;
:Chọn danh mục, mức ưu tiên, SLA và đơn vị xử lý;
:Chọn nhân viên xử lý nếu cần;

|Frontend Next.js|
:Kiểm tra dữ liệu bắt buộc;
if (Dữ liệu hợp lệ?) then (Có)
  :Gửi yêu cầu tạo Ticket;
else (Không)
  :Hiển thị lỗi nhập liệu;
  stop
endif

|Backend Django|
:Kiểm tra permission TICKET_CREATE;
if (Có quyền?) then (Có)
else (Không)
  :Trả lỗi 403;
  |Frontend Next.js|
  :Hiển thị không có quyền;
  stop
endif

|Backend Django|
:Kiểm tra khách hàng, tài khoản, đơn vị và nhân viên;
if (Tham chiếu hợp lệ?) then (Có)
else (Không)
  :Trả lỗi nghiệp vụ;
  |Frontend Next.js|
  :Hiển thị lỗi;
  stop
endif

|Cơ sở dữ liệu|
:Sinh mã Ticket;
:Tạo Ticket với trạng thái CREATED;
:Tạo TicketProcessLog;
:Tạo TicketActivityLog;
if (Có nhân viên được phân công?) then (Có)
  :Tạo TicketAssignment hiện hành;
else (Không)
  :Ticket nằm trong hàng chờ;
endif
if (Có SLA Policy?) then (Có)
  :Tạo SLA Tracking;
  :Tạo các SLA Task theo đơn vị xử lý;
else (Không)
endif

|Backend Django|
if (Có người được phân công?) then (Có)
  :Tạo thông báo phân công;
else (Không)
endif
:Trả Ticket vừa tạo;

|Frontend Next.js|
:Hiển thị thông báo thành công;
:Chuyển đến trang chi tiết Ticket;
stop
@enduml


===== 05_tiep_nhan_xu_ly_ticket.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Activity 05 - Tiếp nhận, phân công và xử lý Ticket

|Nhân viên / Quản lý|
start
:Mở chi tiết Ticket;

|Backend Django|
:Kiểm tra scope và quyền xem Ticket;
if (Được phép truy cập?) then (Có)
else (Không)
  :Trả lỗi 403 / 404;
  stop
endif

|Nhân viên / Quản lý|
if (Ticket chưa có người xử lý?) then (Có)
  :Nhận Ticket hoặc chọn người xử lý;
  |Backend Django|
  :Đóng assignment hiện hành nếu có;
  :Tạo assignment mới;
  :Ghi Activity Log và gửi thông báo;
else (Không)
endif

|Nhân viên / Quản lý|
if (Cần chuyển đơn vị / chi nhánh?) then (Có)
  :Chọn đơn vị, chi nhánh và người nhận mới;
  |Backend Django|
  :Cập nhật đơn vị xử lý;
  :Đóng assignment cũ và tạo assignment mới;
  :Cập nhật SLA theo đơn vị;
  :Gửi thông báo chuyển Ticket;
else (Không)
endif

|Nhân viên / Quản lý|
:Nhập nội dung xử lý / giải pháp / ghi chú;
:Chọn trạng thái tiếp theo;

|Backend Django|
:Kiểm tra chuyển trạng thái hợp lệ;
if (Trạng thái đích?) then (ACCEPTED)
  :Ghi nhận thời điểm tiếp nhận;
elseif (PROCESSING)
  :Ghi nhận bắt đầu xử lý;
elseif (DONE_WAIT_CLOSE)
  :Ghi nhận hoàn tất xử lý;
elseif (CLOSED)
  :Kiểm tra Ticket có quá SLA không;
  if (Quá SLA và chưa có lý do?) then (Có)
    :Yêu cầu nhập lý do quá hạn;
    stop
  else (Không)
    :Đóng Ticket;
    :Đóng Process Log và Assignment;
    :Hoàn tất SLA Tracking;
  endif
else (CANCELLED)
  :Hủy Ticket;
  :Đóng Process Log và Assignment;
endif

|Cơ sở dữ liệu|
:Cập nhật Ticket;
:Tạo Process Log mới;
:Tạo Activity Log;
:Cập nhật SLA Tracking;

|Backend Django|
:Gửi thông báo thay đổi trạng thái;
:Trả Ticket mới nhất;

|Frontend Next.js|
:Cập nhật giao diện và lịch sử xử lý;
stop
@enduml


===== 06_giam_sat_sla_tu_dong_dong.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Activity 06 - Quét SLA, cảnh báo quá hạn và tự đóng Ticket

|Celery Beat|
start
:Kích hoạt task quét SLA định kỳ;

|Celery Worker|
:Thử lấy distributed lock;
if (Task khác đang chạy?) then (Có)
  :Bỏ qua lần quét;
  stop
else (Không)
endif

|Backend SLA|
:Lấy các SLA Tracking chưa hoàn tất;
while (Còn Ticket cần kiểm tra?) is (Có)
  :Tính số phút quá hạn;
  if (Đã quá hạn?) then (Có)
    :Đánh dấu SLA = OVERDUE;
    :Ghi thời điểm breached_at;
    :Tạo cảnh báo quá hạn;
    if (Quá hạn >= 72 giờ?) then (Có)
      :Tạo cảnh báo mức nghiêm trọng;
    else (Không)
    endif
    :Duyệt các Escalation Rule phù hợp;
    :Xác định người nhận;
    :Tạo Ticket Reminder / Notification;
  else (Không)
  endif
endwhile (Không)

|Backend SLA|
:Lấy Ticket ở DONE_WAIT_CLOSE quá thời gian chờ;
while (Còn Ticket cần tự đóng?) is (Có)
  :Chuyển trạng thái sang CLOSED;
  :Đóng Process Log và Assignment;
  :Ghi Activity Log tự động;
  :Hoàn tất SLA Tracking;
endwhile (Không)

|Celery Worker|
:Giải phóng distributed lock;
:Trả thống kê checked / overdue / alert / auto-closed;
stop
@enduml


===== 07_dong_bo_chatbot_tao_ticket.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Activity 07 - Đồng bộ Chatbot và tự động tạo Ticket

|Celery Beat|
start
:Kích hoạt task đồng bộ Chatbot;

|Celery Worker|
:Thử lấy khóa đồng bộ;
if (Task khác đang chạy?) then (Có)
  :Bỏ qua lần đồng bộ;
  stop
else (Không)
endif

|Supabase|
:Trả ChatLog, ChatbotState và CSKH Request mới;

|Backend Chatbot|
:Upsert dữ liệu theo external_id;
:Tập hợp các session_id bị thay đổi;
:Nhóm log, state và request theo session;
while (Còn session cần tổng hợp?) is (Có)
  if (Có CSKH Request?) then (Có)
    :Gán outcome = CCC;
  elseif (Có ChatbotState?) then (Có)
    :Gán outcome = PENDING;
  elseif (Phiên chỉ gồm câu spam?) then (Có)
    :Gán outcome = SPAM;
  else (Không)
    :Gán outcome = BOT_DONE;
  endif
  :Tạo / cập nhật ChatbotSessionSummary;

  if (Outcome = CCC?) then (Có)
    if (Session đã liên kết Ticket?) then (Có)
      :Giữ Ticket hiện có;
    else (Không)
      :Tìm khách hàng theo tài khoản / điện thoại / email;
      :Xây dựng nội dung hội thoại đầy đủ;
      |Backend Ticket|
      :Tạo Ticket nguồn Chatbot;
      :Đặt trạng thái CREATED;
      :Đưa vào hàng chờ CCC nếu chưa gán owner;
      |Backend Chatbot|
      :Liên kết session với Ticket;
    endif
  else (Không)
  endif
endwhile (Không)

|Celery Worker|
:Giải phóng khóa;
:Trả thống kê đồng bộ và số Ticket tạo mới;
stop
@enduml


===== 08_import_phan_loai_loi.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Activity 08 - Import và phân loại lỗi bên ngoài

|Người dùng|
start
if (Nguồn dữ liệu?) then (Nhập thủ công)
  :Nhập ngày nhận, ngày hoàn thành, nguồn, thiết bị,
kết quả xử lý và nội dung;
else (Excel)
  :Chọn file Excel;
  :Chọn tự động phân loại hoặc chỉ import;
endif

|Frontend Next.js|
:Kiểm tra định dạng cơ bản;
:Gửi dữ liệu / multipart file;

|Backend Import|
:Kiểm tra quyền import / tạo lỗi;
if (Có quyền?) then (Có)
else (Không)
  :Trả lỗi 403;
  stop
endif

if (Nguồn = Excel?) then (Có)
  :Đọc workbook và tìm cột theo header;
  :Tạo Import Batch;
  while (Còn dòng dữ liệu?) is (Có)
    :Chuẩn hóa ngày giờ và nội dung;
    :Kiểm tra trường bắt buộc;
    if (Dòng hợp lệ?) then (Có)
      :Tạo ExternalErrorRecord chờ phân loại;
    else (Không)
      :Ghi dòng bị bỏ qua và lý do;
    endif
  endwhile (Không)
else (Không)
  :Chuẩn hóa dữ liệu thủ công;
  :Tạo ExternalErrorRecord;
endif

if (Bật tự động phân loại?) then (Có)
  |Bộ phân loại AI|
  :Làm sạch và che dữ liệu nhạy cảm;
  :Phân loại nhóm lỗi và mã lỗi;
  :Phân loại nhóm nguyên nhân;
  :Trả nhãn và độ tin cậy;
  |Backend Import|
  if (Kết quả đủ tin cậy?) then (Có)
    :Đặt trạng thái CLASSIFIED;
  else (Không)
    :Đặt trạng thái NEED_REVIEW;
  endif
else (Không)
  :Đặt trạng thái chờ phân loại;
endif

|Người dùng|
:Kiểm tra kết quả;
if (Đồng ý kết quả?) then (Có)
  :Xác nhận mã lỗi và nguyên nhân;
else (Không)
  :Chọn lại danh mục hoặc yêu cầu phân loại lại;
endif

|Backend Import|
:Lưu người xác nhận và thời điểm xác nhận;
:Xóa cache dashboard liên quan;
stop
@enduml


===== 09_sale_admin_ghi_nhan_cuoc_goi.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Activity 09 - Ghi nhận và cập nhật cuộc gọi Sale Admin

|Nhân viên Sale Admin|
start
:Mở form ghi nhận cuộc gọi;
:Chọn khách hàng và tài khoản;
:Nhập ngày gọi, lần follow, kết quả, mức quan tâm,
ICP, ghi chú và PIC;

|Frontend Next.js|
:Kiểm tra dữ liệu bắt buộc;
if (Dữ liệu hợp lệ?) then (Có)
  :Gửi yêu cầu tạo / cập nhật SA Record;
else (Không)
  :Hiển thị lỗi nhập liệu;
  stop
endif

|Backend Sale Admin|
:Kiểm tra role và permission;
if (Được phép ghi nhận?) then (Có)
else (Không)
  :Trả lỗi 403;
  stop
endif

:Kiểm tra trùng theo tài khoản + PIC + ngày gọi + lần follow;
if (Bị trùng?) then (Có)
  :Trả lỗi bản ghi đã tồn tại;
  |Frontend Next.js|
  :Hiển thị lỗi trùng;
  stop
else (Không)
endif

|Cơ sở dữ liệu|
if (Thao tác?) then (Tạo mới)
  :Sinh mã SA Record;
  :Lưu bản ghi cuộc gọi;
  :Tạo Audit Log CREATE;
else (Cập nhật)
  :Lưu giá trị trước và sau;
  :Cập nhật bản ghi;
  :Tạo Audit Log UPDATE;
endif

|Backend KPI|
:Gửi yêu cầu tính lại KPI của PIC trong kỳ liên quan;
:Tính lại Actual Value / Score nếu kỳ đang hoạt động;

|Backend Sale Admin|
:Trả bản ghi mới nhất;
|Frontend Next.js|
:Hiển thị thông báo thành công;
:Chuyển đến chi tiết / danh sách SA Record;
stop
@enduml


===== 10_khoi_tao_kich_hoat_ky_kpi.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Activity 10 - Khởi tạo, cấu hình và kích hoạt kỳ KPI

|KPI Admin|
start
:Chọn tháng, năm và hồ sơ KPI;

|Frontend Next.js|
:Gửi yêu cầu lấy / tạo kỳ KPI;

|Backend KPI|
if (Kỳ KPI đã tồn tại?) then (Có)
  :Trả kỳ hiện có;
else (Không)
  :Tạo kỳ KPI trạng thái DRAFT;
  :Khởi tạo Profile mặc định;
  :Khởi tạo Section, Group và Metric;
  :Khởi tạo Gate Config và Reward Tier;
endif

|KPI Admin|
:Cấu hình metric, trọng số, gate và mức thưởng;
:Bấm lưu cấu hình;

|Backend KPI|
:Kiểm tra kỳ có ở trạng thái DRAFT không;
if (Được phép sửa?) then (Có)
else (Không)
  :Trả lỗi kỳ đã khóa / đóng;
  stop
endif
:Kiểm tra tổng trọng số và cấu trúc metric;
if (Cấu hình hợp lệ?) then (Có)
  :Lưu cấu hình;
else (Không)
  :Trả danh sách lỗi cấu hình;
  |Frontend Next.js|
  :Hiển thị lỗi và yêu cầu chỉnh sửa;
  stop
endif

|KPI Admin|
:Bấm kích hoạt kỳ KPI;

|Backend KPI|
:Kiểm tra lại cấu hình và quyền kích hoạt;
if (Đủ điều kiện kích hoạt?) then (Có)
  :Chuyển trạng thái kỳ sang ACTIVE;
  :Ghi người kích hoạt và thời điểm;
else (Không)
  :Giữ trạng thái DRAFT và trả lỗi;
  stop
endif

|Frontend Next.js|
:Hiển thị kỳ KPI đang hoạt động;
stop
@enduml


===== 11_thiet_lap_chi_tieu_kpi.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Activity 11 - Thiết lập chỉ tiêu KPI cho nhân viên

|KPI Admin / SA Supervisor|
start
:Chọn kỳ KPI và Profile;

|Frontend Next.js|
:Gọi API lấy ma trận nhân viên x Metric;

|Backend KPI|
:Kiểm tra scope quản lý;
:Lấy danh sách nhân viên thuộc phạm vi;
:Lấy Metric có thể đặt target;
:Trả ma trận chỉ tiêu;

|KPI Admin / SA Supervisor|
if (Cách thiết lập?) then (Nhập trực tiếp)
  :Nhập Target Value và ghi chú cho từng ô;
elseif (Sao chép kỳ trước) then (Kỳ trước)
  :Chọn sao chép target từ kỳ trước;
else (Nhân viên mẫu)
  :Chọn nhân viên nguồn và nhân viên đích;
endif
:Bấm lưu hàng loạt;

|Frontend Next.js|
:Kiểm tra số liệu và gửi bulk update / copy request;

|Backend KPI|
:Kiểm tra permission KPI_TARGET_MANAGE;
if (Có quyền?) then (Có)
else (Không)
  :Trả lỗi 403;
  stop
endif
:Kiểm tra kỳ có cho phép chỉnh target;
if (Được chỉnh?) then (Có)
else (Không)
  :Trả lỗi kỳ đã khóa / đóng;
  stop
endif
:Kiểm tra nhân viên và Metric thuộc đúng Profile / scope;
if (Dữ liệu hợp lệ?) then (Có)
  :Tạo mới hoặc cập nhật KPI Target;
  :Ghi người cập nhật và thời điểm;
else (Không)
  :Trả chi tiết dòng / ô bị lỗi;
  stop
endif

|Frontend Next.js|
:Tải lại ma trận chỉ tiêu;
:Hiển thị kết quả cập nhật;
stop
@enduml


===== 12_tinh_diem_xep_hang_kpi.puml =====

@startuml
skinparam backgroundColor transparent
skinparam shadowing false
skinparam defaultFontName Arial
skinparam activity {
  BackgroundColor #F8F9FA
  BorderColor #495057
  DiamondBackgroundColor #FFF3BF
  DiamondBorderColor #E67700
  StartColor #2B8A3E
  EndColor #C92A2A
  BarColor #495057
  FontColor #212529
}
skinparam ArrowColor #495057
skinparam NoteBackgroundColor #FFF9DB
skinparam NoteBorderColor #E67700

title Activity 12 - Tính điểm, tổng hợp và xếp hạng KPI

|Hệ thống / Người chấm|
start
:Chọn kỳ KPI, Profile và nhân viên;

|Backend KPI|
:Lấy Metric và Target của nhân viên;
while (Còn Metric cần tính?) is (Có)
  if (Loại Metric?) then (Tự động)
    |Nguồn dữ liệu nghiệp vụ|
    :Tổng hợp SA Record / Transaction / dữ liệu liên quan;
    |Backend KPI|
    :Tính Actual Value;
  else (Thủ công)
    |Người chấm|
    :Nhập Actual Value / Score và ghi chú;
    |Backend KPI|
    :Kiểm tra quyền chấm và scope;
  endif
  :Áp dụng công thức tính điểm Metric;
  :Giới hạn điểm theo cấu hình nếu có;
  :Lưu KPI User Metric Result;
endwhile (Không)

|Backend KPI|
:Tổng hợp điểm Metric thành Group;
:Tổng hợp Group thành Section;
:Tính kết quả Gate;
if (Không đạt Gate bắt buộc?) then (Có)
  :Áp dụng giới hạn / điều chỉnh điểm thưởng;
else (Không)
endif
:Tính Total Score;
:Xác định Reward Tier;
:Lưu KPI User Summary;

|Backend KPI|
:Lấy Summary của các nhân viên cùng kỳ / Profile;
:Sắp xếp theo Total Score;
:Xử lý đồng hạng theo quy tắc hệ thống;
:Trả bảng xếp hạng;

|Frontend Next.js|
:Hiển thị KPI cá nhân, chi tiết điểm và ranking;
stop
@enduml


===== 13_quan_ly_ekyc.puml =====

@startuml
title Activity 13 - Quản lý, import và chăm sóc eKYC
|Nhân viên CCC / CS|
start
:Mở phân hệ quản lý eKYC;
if (Hình thức ghi nhận?) then (Nhập thủ công)
  :Tra cứu khách hàng theo số tài khoản;
  :Nhập ngày gọi, follow, PIC, tình trạng, kết quả và ghi chú;
  |Backend eKYC|
  :Kiểm tra quyền, chuẩn hóa và lưu EkycRecord;
else (Import Excel)
  :Đọc workbook và ánh xạ cột;
  while (Còn dòng dữ liệu?) is (Có)
    :Chuẩn hóa và kiểm tra dòng;
    :Tra cứu khách hàng và tạo EkycRecord;
  endwhile (Không)
  :Trả kết quả import theo dòng;
endif
|Frontend Next.js|
:Hiển thị danh sách, bộ lọc và phân trang;
|Nhân viên CCC / CS|
:Cập nhật kết quả chăm sóc nếu cần;
if (Tác vụ?) then (Dashboard)
  :Xem KPI và biểu đồ eKYC;
elseif (Export)
  :Tải file Excel theo bộ lọc;
else (Kết thúc)
endif
stop
@enduml


===== 14_xu_ly_failed_ekyc.puml =====

@startuml
title Activity 14 - Tiếp nhận và xử lý Failed eKYC
|Nhân viên CCC / Admin|
start
:Mở phân hệ Failed eKYC;
|Backend Django|
:Kiểm tra role Admin / CCC;
if (Được phép?) then (Có)
else (Không)
  :Trả lỗi 403;
  stop
endif
|Nhân viên CCC / Admin|
if (Nguồn dữ liệu?) then (Nhập thủ công)
  :Nhập Step, lỗi và thông tin khách hàng nếu có;
  |Backend Failed eKYC|
  :Chuẩn hóa và lưu FailedEkycRecord;
else (Import Excel)
  :Đọc workbook và ánh xạ header;
  while (Còn dòng dữ liệu?) is (Có)
    if (Dòng hoàn toàn trống?) then (Có)
      :Bỏ qua;
    else (Không)
      :Chuẩn hóa Step, ngày và follow;
      :Cho phép Account, Email, Phone trống;
      :Đưa bản ghi vào danh sách tạo;
    endif
  endwhile (Không)
  :Tạo hàng loạt trong transaction;
endif
|Nhân viên CCC / Admin|
:Cập nhật PIC, ngày gọi, follow,
tình trạng, kết quả và CS comment;
:Xem Dashboard hoặc export theo bộ lọc;
stop
@enduml


===== 15_quan_ly_khao_sat_csat.puml =====

@startuml
title Activity 15 - Ghi nhận và quản lý khảo sát CSAT
|Nhân viên CCC / Admin|
start
:Mở phân hệ Khảo sát;
|Backend Survey|
:Kiểm tra quyền và phạm vi ticket;
if (Được phép?) then (Có)
else (Không)
  :Trả lỗi 403;
  stop
endif
|Nhân viên CCC / Admin|
if (Nguồn dữ liệu?) then (Nhập thủ công)
  :Tra cứu và chọn ticket của khách
  trong tháng gửi khảo sát;
  :Nhập trạng thái gửi, điểm và mô tả;
else (Import Excel)
  :Tải file và xem trước dữ liệu;
  |Backend Survey|
  :Chuẩn hóa dòng và gợi ý ticket;
  |Nhân viên CCC / Admin|
  :Xác nhận ticket cho từng dòng;
endif
|Backend Survey|
while (Còn dòng cần ghi?) is (Có)
  if (Ticket đã khảo sát thành công?) then (Có)
    :Ghi lỗi trùng khảo sát;
  else (Không)
    :Lưu TicketSurveyLog;
    if (Gửi thành công?) then (Có)
      :Cập nhật TicketFeedback;
    else (Thất bại)
      :Cho phép gửi lại;
    endif
    :Tạo Audit Log;
  endif
endwhile (Không)
|Frontend Next.js|
:Hiển thị danh sách và bộ lọc;
|Nhân viên CCC / Admin|
if (Tác vụ?) then (Cập nhật)
  :Sửa kết quả và xem lịch sử;
elseif (Dashboard) then (Dashboard)
  :Xem KPI, CSAT và biểu đồ so sánh;
else (Kết thúc)
endif
stop
@enduml
